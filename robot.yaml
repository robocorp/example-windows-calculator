tasks:
  Id Based Calculator Demo:
    shell: python -m robot --report NONE --outputdir output --logtitle "Task log" id-based.robot

  Image Based Calculator Demo:
    shell: python -m robot --report NONE --outputdir output --logtitle "Task log" image-based.robot

condaConfigFile: conda.yaml
artifactsDir: output
PATH:
  - .
PYTHONPATH:
  - .
ignoreFiles:
  - .gitignore
